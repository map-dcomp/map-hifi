The client driver reads the same files as the lo-fi simulation.

A client instance is started per the numClients property of the ClientRequest.

The client instance is run for the max of the server duration and the network duration of the ClientRequest.
